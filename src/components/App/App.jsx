import { AppContainer } from './App.styled';
import { Section } from 'components/Section/Section';

export const App = () => {
  return (
    <AppContainer>
      <Section />
    </AppContainer>
  );
};
