import {
  BtnContainer,
  Button,
} from './FeedbackOptions.styled';

export const FeedbackOptions = () => {
  return (
    <BtnContainer>
      <Button>Good </Button>
      <Button>Neutral</Button>
      <Button>Bad</Button>
    </BtnContainer>
  );
};
